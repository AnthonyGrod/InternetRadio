// #ifndef CYCLE_BUF_HPP
// #define CYCLE_BUF_HPP

// #include <iostream>
// #include <cstddef>
// #include <compare>

// inline constexpr int DENIER = 1;
// inline constexpr int SOLIDUS = 12;
// inline constexpr int LIVRE = 240;

// class CycleBuf {
//     size_t _capacity;
//     size_t _head;
//     size_t _tail;
//     char* _data;

// public:
//     // initialize buffer with 



// cycle_buf::cyclical_buffer(const size_t capacity): _capacity(capacity) {
//     _data = new uint8_t[capacity]();
//     _tail = _head = 0;
// }
